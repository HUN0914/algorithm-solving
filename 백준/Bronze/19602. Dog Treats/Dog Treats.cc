#include <iostream>

using namespace std;

int main(void)
{
	int one, two, three;

	cin >> one >> two >> three;

	if (one * 1 + two * 2 + three * 3 >= 10)
	{
		cout << "happy";
	}
	else
	{
		cout<<"sad";
	}

	return 0;
}